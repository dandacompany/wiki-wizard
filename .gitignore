# Meta/planning artifacts excluded from distribution (ignore everything under docs/
# except the published tutorial showcase + the full-feature reference)
docs/*
!docs/tutorial-omw/
!docs/tutorial-reference/

# Claude Code / AI agent instructions and memory (local-only, never publish)
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
MEMORY.md
memory/
.claude/

# Secrets / env
.env
.env.*
!.env.example

# IDE / editor config
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Runtime state (per-user sqlite)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/*.migrated
!data/.gitkeep

# Virtualenv
.venv/

# Editable install metadata
*.egg-info/

# OS
.DS_Store

# uv (not used here)
uv.lock

# Per-vault runtime state (autoresearch sessions, hot cache state, etc.)
.oh-my-wiki/
