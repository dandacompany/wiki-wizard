# Baseline every page type extends. Override per-type or per-vault.
required_fields: [title, date, type, tags]
field_types:
  tags: list
  title: str
  date: str
  review: dict
required_sections: []
allowed_values:
  confidence: [high, medium, low]
  status: [draft, inbox, processed, raw, superseded, meta]
